version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto8
  build:
    commands:
      - echo "build 1"
      - echo "CODEBUILD_SOURCE_VERSION"
      - echo $CODEBUILD_SOURCE_VERSION
